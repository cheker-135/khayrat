{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "chmod -R 775 storage bootstrap/cache && php artisan storage:link && php -S 0.0.0.0:$PORT -t public"
  }
}