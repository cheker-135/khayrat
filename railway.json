{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "chmod -R 777 storage bootstrap/cache public && mkdir -p storage/app/public/photos/shares storage/app/public/files/shares && rm -rf public/storage storage/framework/sessions/* && ln -s ../storage/app/public public/storage && echo '--- Storage Link Info ---' && ls -ld public/storage && ls -F public/storage && php artisan optimize:clear && php artisan vendor:publish --tag=lfm_public --force && php -S 0.0.0.0:$PORT -t public"
  }
}