{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "chmod -R 777 storage bootstrap/cache public && rm -rf public/storage && ln -s ../storage/app/public public/storage && php artisan optimize:clear && php artisan vendor:publish --tag=lfm_public --force && php -S 0.0.0.0:$PORT -t public"
  }
}