{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "chmod -R 777 storage bootstrap/cache && rm -rf public/storage && php artisan storage:link && php artisan vendor:publish --tag=lfm_public --force && php artisan optimize:clear && php -S 0.0.0.0:$PORT -t public"
  }
}