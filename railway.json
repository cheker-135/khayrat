{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p storage/app/public/photos/shares storage/app/public/files/shares storage/framework/views storage/framework/sessions storage/framework/cache && chmod -R 775 storage bootstrap/cache && php artisan storage:link --force && php artisan migrate --force && php artisan optimize:clear && php artisan vendor:publish --tag=lfm_public --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}